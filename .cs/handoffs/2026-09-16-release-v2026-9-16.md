---
parent: a5912573-14ec-42d6-b3b1-66d25abdaa8d
created: 2026-09-16T15:23:00Z
purpose: run the held release v2026.9.16 (task #622) — 126 commits of content on main, unpushed, CHANGELOG Unreleased ready
status: unconsumed
---

## 1. Next Step

Run the release. `/release` is the command (`.claude/commands/release.md`,
read in source this conversation); Alex's shape for it is memory
`project_release_gate_skips_ci`: **skip the repeat local run, push the release
commit, and tag ONLY after that commit's CI jobs are green.**

State at rotation, all measured just now:

- main is at `75ea47c`, clean apart from machine-local `.cs/` files.
- **126 commits ahead of `origin/main`, nothing pushed.** 154 commits since
  the newest tag, `v2026.9.15`.
- `VERSION="2026.9.15"` in two places, `bin/cs:8` and `lib/00-header.sh:8`
  (`bin/cs` is generated by `./build.sh` from `lib/`, so edit the lib and
  rebuild — memory `project_lib_bin_build_drift`).
- `CHANGELOG.md` has a populated `## Unreleased` with Changed / Features /
  Fixes / Docs sections. Today's two merges added entries there: the wrap key
  (Features) and the git-segment subdirectory fix (Fixes, first entry).
- **`scratchpad/release-notes.md` does NOT exist.** A prior handoff said notes
  were drafted there; that scratchpad belonged to an older conversation's
  working tree and is gone. Draft notes from the CHANGELOG instead.
- Everything merged today is installed locally and `cs -doctor` reports
  deploy drift OK.

The two standing doctor WARNs are expected and not release blockers: the
sidebar bridge holding the status-line slot, and a shadow-ref warning for this
conversation's uncommitted `.cs/` files.

Alex has not said "release now" in this conversation. He listed #622 as next
in priority twice. Ask him at the gate the release command itself defines
(its step 0 preflight is an AskUserQuestion when main is not synced with
origin — and main is 126 ahead, so that gate will fire).

After the release, his stated order is **#603 and #609**, the two parallel-run
test races, which he said would go well in one branch — and **#638**, filed
today, belongs with them (same class: load-induced ghost flakes).

## 2. Settled and rejected

**Settled this conversation:**

- The wrap key's guard, decided by me and not asked (Alex was winding down):
  `2` arms, `3` beside it confirms. Two earlier shapes were built and
  **rejected on evidence**, in this order:
  1. *Two presses of `2` within five seconds.* Rejected: a held key repeats on
     keydown (the mods contract says so), so a held `2` would confirm itself.
  2. *A 400 ms settle window ignoring presses just after arming.* Rejected by
     Codex round 2 and agreed: macOS's first key repeat lands 225 ms or more
     after the keydown, which is inside a human double-press, so **no elapsed
     time separates a repeat from a deliberate second press.** The mechanism
     was wrong, not its constant.
  3. *Removing the button from `2` while armed.* Rejected on a live
     measurement (below): the digit then fell through to the composer.
- `/wrap` is two Opus passes plus a shell helper (`cs -narrative rotate`), not
  three Opus passes. My first docs wording said three; corrected everywhere.
- The git-segment fix climbs to the nearest `.git` rather than following a
  project pointer. Task #633 had already rejected the pointer idea (adopted
  and worktree sessions already show the checkout's branch); this is the
  different, real bug underneath Alex's screenshot.

**Rejected earlier, still load-bearing:** see the Key Technical Concepts
section of `.cs/handoffs/2026-09-16-wrap-key.md` for the mods contract facts,
and `.cs/handoffs/2026-09-16-hint-mod.md` for the PromptHint findings.

## 3. Conversation-only facts

These exist nowhere else. Everything else here is recoverable from git, the
narrative, or memory.

- **MEASURED on Claude Code 2.1.273, cost one full build-and-live cycle:** a
  `<Button>` nested inside a `<Text>` in a mod's `AbovePrompt` tree is refused
  by the engine, and **the whole band vanishes silently.** The only trace is
  one debug-log line: `ui.render (AbovePrompt): a hook returned a tree that
  does not validate (Button inside an inline element); drawing the engine's
  own`. All 60 bun tests stayed green throughout — the fake engine validates
  no structure. Saved as memory `project_mod_button_inline_refused`.
- **MEASURED live, and it reversed a design Codex had already passed:** with
  no `Button` on `2` while the wrap key was armed, pressing `2` a second time
  **typed the character into the composer** (the pane read `223`), and a
  non-empty composer takes every band hotkey with it (the contract: a digit
  presses from an EMPTY composer). Codex round 3 had verified the same code as
  MERGE from the contract alone and explicitly flagged composer routing as not
  live-reproduced. The fix keeps a re-arming button on `2`.
- **MEASURED, the answer to Alex's "why don't we have a branch here?"**: the
  bar's git capsule was blank because my own Bash tool had `cd`'d into
  `mods/cs-rotate`. `workspace.current_dir` follows the Bash tool's working
  directory, and the segment probed `$dir/.git` in that directory alone.
  Fixed and merged today (`75ea47c`).
- **Two throwaway-session traps, both cost real time:**
  1. `pgrep -f 'claude --name X'` matches **the Bash tool's own zsh**, because
     its argv carries the command text. `kill $(pgrep …)` killed my own shell
     and left the claude alive. Kill by
     `ps -Ao pid=,args= | grep -F "name X"` in a *separate* tool call.
  2. cs's live-duplicate guard (`lib/75-launch.sh:182`) scans all argv, so a
     single Bash call whose text contains `--name X --session-id` *and*
     launches `cs X` trips the guard **on itself** — "already running
     elsewhere". Launch from a command whose text carries neither string.
     This is #626's class, reappearing from a different direction.
- **Right after a `/resume` answered with `y`, three `2` presses typed into
  the composer** instead of pressing the band: the resume's own turn had not
  finished. With the composer cleared and the turn over, the same keys pressed
  the band. Not a mod defect; worth knowing when demoing.
- **`codex exec` hangs on "Reading additional input from stdin..."** without
  `</dev/null`. Round 1 sat there for ten minutes and wrote a zero-byte file.
  Memory `project_hook_subprocess_eats_stdin` covers the hook case; it applies
  to `codex exec` too.
- **Ghost flakes seen today, filed as #638** (all on unrelated branches, so
  not caused by this work): `test_run_all.sh
  test_run_all_reports_the_failing_suite_by_name` reported "wrong failure
  tally" **three separate times today** while its own dump showed
  `[5/5] test_fake3.sh 0s FAIL` and `FAILED: 1/5 suites`; and
  `test_scope_prompt.sh` took 196 s on a loaded ghost and its classifier
  corpus got the slow-machine skip line despite `CS_SCOPE_BUDGET_MS=600000`.
  Ghost keeps ONE `suite.log` — read a failure before launching the next run.
- **Appending a `run_test` line AFTER `report_results` in a shell suite** runs
  that test after the tally: the suite printed `Results: 232/232 passed` with
  a `FAIL:` line below it. The block must sit above `report_results`.
- **Alex's exact words this conversation, in order:** *"why don't we have a
  branch here?"* (screenshot of the bar with no git capsule); *"what do we
  still have open as tasks"*; *"how about the statusline branch display
  issue?"*; *"merge it when ghost and codex are green"*; then `/rotate`.
  At the wrap-key merge gate he picked "Merge now"; at the wrap-up prompt he
  picked "Not yet — keep working".
- **Counts at rotation:** ghost ran 8 times today across the two branches
  (67/67 every time except run 4, the #638 flakes); Codex ran 7 read-only
  rounds (4 on the wrap key, 3 on the git segment), each round's report saved
  under `scratchpad/codex/out-*.md`.

## Completeness (pass one)

Written from live context at ~43%, no compaction. Pass one carries everything
that dies with the conversation. Pass two appends the recoverable sections.
