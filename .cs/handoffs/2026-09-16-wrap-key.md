---
parent: ba05bd56-aa82-41fe-afad-93f8eb27c321
created: 2026-09-16T14:13:06Z
purpose: build a one-key /wrap on the cs-rotate band, beside the existing rotate key, with a guard against an accidental press
status: unconsumed
---

## 1. Next Step

Nothing is in flight. Main is at `a92fe4b`, clean apart from machine-local
`.cs/` files, 112 commits ahead of origin, nothing pushed. Everything this
conversation built is merged and installed.

Alex's words, in full: *"do we have a /wrap equivalent for this?"* with a
screenshot of the band reading `1: rotate this conversation · 59%`, then,
as the argument to `/rotate`: *"and build it"*.

Build a second key on the `cs-rotate` band that runs `/wrap`. Branch off
main (`feat/wrap-key` or similar). The design is NOT settled beyond what
section 2 records — Alex approved building it, not a specific shape, and the
guard question below is a real decision you should make and state, not ask
about (he is winding down).

Order:

1. Read `mods/cs-rotate/hooks/register.tsx` (the band is the `ui.render`
   hook on `AbovePrompt`, one `Box` capsule containing a `Button` with
   `hotkey="1"` and `plain`, which draws as `1: <label>`), its test at
   `mods/cs-rotate/test/register.test.ts` (54 bun tests against a fake
   engine), and `tests/test_mod_rotate.sh`.
2. TDD in `mods/cs-rotate/test/register.test.ts` with the fake engine, one
   fact per red→green cycle. At minimum: the second button appears only where
   the band already draws (lead, idle, not working), its press runs
   `{ command: 'wrap', args: '' }`, and whatever guard you choose.
3. `tests/test_mod_rotate.sh` has a pin that WILL fail and must be updated
   deliberately, not silently: it asserts the exact `claude plugin validate`
   inventory line `$.command.run (via clearAndContinue, rotate)`. A third
   call site changes that string. The same test also pins the hooks list.
4. `docs/hooks.md` cs-rotate section, `README.md` (the conversation-rotation
   bullet names the band's key), `CHANGELOG.md` Unreleased → Features.
   `./build.sh` last before committing (`project_lib_bin_build_drift`).
5. Gate as every change this week was: ghost full suite
   (`bash /Users/alex.geana/.claude/plugins/cache/hex-plugins/claude-tmux/2026.9.1/scripts/remote-tests.sh --host ghost@ghost </dev/null`,
   `ssh ghost@ghost 'rm -f ci/claude-sessions/suite.status'` FIRST, poll from
   a background loop; the suite is 67 now, not 68), one or two Codex
   read-only rounds, a live check in a throwaway cs session, then
   AskUserQuestion at the merge gate with polish-first as the first option
   (Alex has picked polish-first 8 times out of 8).

After this, the standing queue in Alex's stated priority: **#622 the held
release v2026.9.16** (112 commits unpushed now), then **#603** and **#609**,
the two parallel-run test races, which he noted would go well in one branch.

## 2. Settled and rejected

**Settled this conversation, before the build was asked for:**

- The band draws exactly ONE key today, `hotkey="1"`: `rotate this
  conversation`, or `/clear and continue from the handoff` once a handoff is
  armed (read in source, `mods/cs-rotate/hooks/register.tsx`).
- `/wrap` is `commands/wrap.md`: `model: opus` in its frontmatter, and three
  passes in order — memory distillation (reads `~/.claude/commands/sweep.md`),
  session summary (reads `summary.md`, steps 1-4 only, REPLACES
  `.cs/summary.md`), then the narrative rotation. Read in source this
  conversation.
- What I told Alex, which is the reasoning he approved on: the band's rotate
  key does roughly the last third of a wrap and skips the memory and summary
  passes. A wrap key is cheap to add, but two things make it less obviously
  right than the rotate key — `/wrap` REPLACES the summary file rather than
  appending, and it runs three Opus passes, so it costs minutes and real
  tokens. An accidental wrap is a worse mistake than an accidental rotation.
  I said I would want it labelled plainly and placed where a mis-hit is
  unlikely. He said "and build it" without narrowing that, so the guard is
  yours to design and state.
- Guard shapes worth considering (none tried, none rejected): a hotkey that
  is not adjacent to `1`; no hotkey at all, so it is mouse-only; a two-step
  press (first press arms the label, second runs); drawing it only past a
  higher context threshold than the rotate key's 40. `$.ui.toast` exists if
  you want a confirmation that costs nothing.

**Rejected earlier this conversation, unrelated to the key but load-bearing
for anyone touching this mod:** see section 4's contract facts.

## 3. Conversation-only facts

These exist nowhere else. Everything else in this handoff is recoverable from
git, the narrative or memory.

- **MEASURED on Claude Code 2.1.273, and it cost a full build-and-debug
  cycle:** a `ui.render{component=PromptHint}` hook that returns
  `next({ ...e, props: { ...e.props, hint } })` draws NOTHING while the
  permission-mode notice (`⏵⏵ auto mode on (shift+tab to cycle)`) owns the
  engine's line — which in a cs session it always does, since the mode cycle
  is auto/manual/accept-edits/plan and none of them shows `? for shortcuts`.
  The hook settled every render (2.0-2.7 ms, visible in the debug log) and
  the engine refused nothing; the footer simply never changed. Returning a
  TREE from the same hook draws it as an extra footer row BENEATH the notice.
  Saved as memory `project_prompthint_rewrite_draws_nothing`. The method that
  found it: launch with `CLAUDE_CODE_BIN='~/.local/bin/claude --debug' cs <name>`
  (the launch word-splits `$CLAUDE_CODE_BIN`, so flags ride along) and read
  `~/.claude/debug/<session-uuid>.txt`.
- **The handoff that started this conversation carried a false premise, and
  it reached the shipped code before a reviewer caught it:** it specified a
  Fable-session tip pointing at `cs -statusline caps` as "the caps consent
  for plan limits". `cs -statusline caps` is the ROUNDED POWERLINE CAPSULE
  ENDS (a font check), not plan limits (read in source, `lib/70-statusline.sh`
  and `lib/10-help.sh:40`). The Fable arm is gone and the tip now describes
  what the command does.
- **The commit-before-mutating rule was broken TWICE in one day**, once in
  each build. Both times a `git checkout <file>` meant to undo a probe `sed`
  wiped uncommitted work: the folded review fixes for the hint mod, and three
  new tests in the scope-prompt suite. The memory
  `feedback_commit_before_mutating` was written after the first and did not
  prevent the second. The sharper rule now recorded there: probe edits go on
  a COPY, and `git checkout <path>` is allowed only when
  `git diff --quiet -- <path>` is true first.
- **`/dev/full` is not writable on macOS** ("operation not permitted"), so a
  `> /dev/full` redirect fails before the command runs and a test using it as
  a failed-write fixture passes vacuously. A CLOSED stdout (`>&-`) makes jq
  fail its write (rc 2) and discriminates. The scope-prompt test proves its
  own fixture live afterwards.
- **An unidentified ghost red I could not chase:** ghost run 8, on commit
  `fceff07` of the scope-prompt branch, reported one failing test in
  `test_scope_prompt.sh`. Its log was overwritten by the next run before I
  read it. Runs 9, 10 and 11 on later shas were all 68/68 clean, and the
  final merged state is green three times over, but I never learned what it
  was. **Ghost keeps ONE `ci/claude-sessions/suite.log`** — read a failure
  before launching the next run.
- **The scope-prompt front half took 1920 ms at load 16** on this machine,
  measured by hand with the budget forced to 0. The default budget is 1500,
  so on a loaded dev box the line reads `Scope: skipped, slow machine`
  instead of grounding the prompt. That is by design (the task forbade making
  the scan cheaper) but it means Alex will see it.
- **Alex's exact words this conversation, in order:** *"did we fix this?"*
  (screenshot of the red `UserPromptSubmit hook timed out after 3s` line);
  *"what's happening ?"*; *"what tasks do we still have opne?"*; *"what is
  623?"*; *"I don't like the new .app notifications thingy, I think that
  should belong to cs iterm-agents-sidebar"*; *"do we have a /wrap equivalent
  for this?"*; *"and build it"*.
- **I gave him a wrong answer once and corrected it:** I listed #623 as open
  from my own summary; the task list says completed. Check `TaskList` rather
  than a recollection.
- **The iterm-agents-sidebar session was messaged twice** and replied asking
  seven questions before it rotated. It has: the exact
  `-group`/`-title`/`-message`/`-activate` invocation and the `-remove`; that
  the lead test is pid-based (`CS_LEAD_PID` vs `CLAUDE_PID`, or its parent),
  which is why nothing file-based can tell a tmux teammate apart; that macOS
  ignores `-appIcon` so a notification wears its SENDER's icon; that a new
  sender bundle of theirs means a fresh permission prompt while iTerm2 itself
  is already approved; and that `~/.local/share/cs/cs.app` is deleted. They
  said they would record it into their own handoff whether or not they act.
- **Two Codex rounds contradicted my own reading of my own test** and were
  right both times: the cs-hint spoken-flag write ran before the ownership
  check (creating `.cs/local` in a plain directory and letting a teammate
  overwrite the lead's flag), and the scope-prompt digest cursor advanced
  even when the emission failed — an inherited bug, not one I introduced.
